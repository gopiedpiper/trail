spring:
  application:
    name: webdoor

feign:
  hystrix:
    enabled: true

management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: hystrix.stream
    
hystrix:
    command:
        default:
            execution:
                isolation:
                    thread:
                        timeoutInMilliseconds: 3000
